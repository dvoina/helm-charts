apiVersion: v1
kind: Service
metadata:
  name: {{ include "ckan.fullname" . }}
  labels:
    {{- include "ckan.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "ckan.fullname" . }}
  ports:
    - name: http
      port: 5000
      targetPort: 5000
